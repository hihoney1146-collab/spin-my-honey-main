/** Re-exports — prefer @/lib/resultProof for new code. */
export {
  encodeProofPayload,
  decodeProofPayload,
  buildProofUrl,
  encodeResultId,
  decodeResultId,
  buildResultProofUrl,
  RESULT_METHOD,
} from "./resultProof";
export type { ResultProofPayload } from "./resultProof";
